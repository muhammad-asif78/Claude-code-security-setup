You are installing the local Trail of Bits starter configuration into the
user's `~/.claude/` directory.

Install these files when requested:

- `settings.json`
- `claude-md-template.md`
- `mcp-template.json`
- `scripts/statusline.sh`
- `commands/review-pr.md`
- `commands/fix-issue.md`

Do not overwrite user customizations without confirmation. Safe-to-overwrite
files include the statusline and command templates.
